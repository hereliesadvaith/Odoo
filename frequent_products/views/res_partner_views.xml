<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Inherit contact page views -->
    <record id="view_partner_form" model="ir.ui.view">
        <field name="name">res.partner.view.form.inherit.associated.products</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='category_id']" position="after">
                <field name="associated_product_ids" widget="many2many_tags"/>
            </xpath>
            <xpath expr="//page[@name='sales_purchases']" position="before" >
                <page string="Frequenct Products">
                    <field name="frequent_product_ids">
                        <tree editable="bottom">
                            <field name="product_id"/>
                            <field name="sale_orders"/>
                            <field name="last_sale_date"/>
                            <field name="last_invoiced_amount"/>   
                        </tree>
                    </field>
                </page>
            </xpath>
        </field>
    </record>
</odoo>
