<?xml version="1.0"?>
<odoo>
    <!--  Inherit chatbot script form view.  -->
    <record id="chatbot_script_view_form" model="ir.ui.view">
        <field name="name">chatbot.script.form.inherit.mcp.server</field>
        <field name="model">chatbot.script</field>
        <field name="inherit_id" ref="im_livechat.chatbot_script_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//widget[@name='web_ribbon']" position="after">
                <widget name="web_ribbon" title="AI Agent" bg_color="text-bg-info" invisible="not is_agent"/>
            </xpath>
            <notebook>
                <page string="Bot Access" name="page_bot_access" invisible="not is_agent">
                    <field name="chatbot_access_ids" widget="one2many" nolabel="1">
                        <list editable="bottom">
                            <field name="ir_model_id"/>
                            <field name="ir_model_fields_ids" widget="many2many_tags"/>
                        </list>
                    </field>
                </page>
            </notebook>
        </field>
    </record>
</odoo>
