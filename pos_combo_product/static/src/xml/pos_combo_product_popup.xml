<?xml version="1.0" encoding="UTF-8"?>
<templates id="template" xml:space="preserve">
	<t t-name="ComboProductPopup" owl="1">
		<div class="popup product-info-popup" style="max-width: 684px;">
			<main class="body">
				<div>
					<header>
						<h3>
							Combo Products
                            <hr/>
						</h3>
							<t t-foreach="props.categories" t-as="category" t-key="category.id">
								<div class="section-title">
									<span t-esc="category.name"/>
								</div>
								<div>
									<article class="product" t-on-click="addToOrderLine">
										<t t-if="props.product.image_128">
											<div class="product-img">
												<img t-att-src="props.product.image_url" t-att-alt="props.product.display_name" />
											</div>
										</t>
										<div class="product-content">
											<div t-attf-class="#{props.product.image_128 ? 'product-name' : 'product-name no-image' }" t-attf-id="article_product_{{props.product.id}}">
												<t t-esc="props.product.display_name" />
											</div>
										</div>
									</article>
								</div>
							</t>
					</header>
				</div>
			</main>
            <footer class="footer cash-move">
				<div class="button confirm disable highlight" t-on-click="confirm">
                    Confirm
                </div>
                <div class="button cancel" t-on-click="cancel">
                    Cancel
				</div>
			</footer>
		</div>
	</t>
</templates>