<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Inherit views for customer creation page -->
    <record id="view_partner_form" model="ir.ui.view">
        <field name="name">base.partner.form.inherit.associated.products</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='barcode']" position="after">
                <field name="enable_purchase_limit"/>
                <field name="purchase_limit_value" attrs="{'invisible': [('enable_purchase_limit', '=', False)]}"/>
            </xpath>
        </field>
    </record>
</odoo>
