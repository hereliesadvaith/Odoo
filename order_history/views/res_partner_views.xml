<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Inherit views for customer creation page -->
    <record id="view_partner_form" model="ir.ui.view">
        <field name="name">base.partner.form.inherit.order.history</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <div class="oe_button_box" name="button_box" position="inside">
                <button type="object" name="action_view_products" class="oe_stat_button"
                    icon="fa-truck" attrs="{'invisible': [('product_count', '=', 0)]}">
                    <field name="product_count" widget="statinfo" string="Products" />
                </button>
            </div>
            <notebook position="inside">
                <page name="Sale Orders">
                    <field name="sale_order_ids" widget="Many2many" nolabel="1">
                        <tree edit="0" create="0" delete="0">
                            <field name="name"/>
                            <field name="date_order"/>
                            <field name="user_id"/>
                            <field name="company_id"/>
                            <field name="amount_total"/>
                            <field name="invoice_status"/>
                        </tree>
                    </field>
                </page>
            </notebook>
        </field>
    </record>
</odoo>